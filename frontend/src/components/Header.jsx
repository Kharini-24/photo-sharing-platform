// A small, consistent header shown across every page -- gives the app a
// single identity instead of feeling like disconnected screens.
export default function Header() {
  return (
    <div className="app-header">
      <span className="app-logo">Frame &amp; Share</span>
    </div>
  );
}
